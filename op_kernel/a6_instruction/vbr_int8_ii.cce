// Generated by benchmarks/a6/generate.py. Edit the generator, then regenerate.
// VBR/int8: No self vector-register RAW path in this form.
// MB_KERNEL bench_vbr_int8_ii
// MB_TENSOR output A int8 4096
// MB_TENSOR input B int8 4096
// MB_TENSOR input C int8 4096
#ifndef __aicore__
#define __aicore__ [aicore]
#endif
#if __NPU_ARCH__ != 9201
#error "A6 probes require --cce-aicore-arch=dav-920r1-vec"
#endif
extern "C" __global__ __aicore__
void bench_vbr_int8_ii(__gm__ int8_t* A, __gm__ int8_t* B, __gm__ int8_t* C)
{
    __ubuf__ int8_t* ubB = (__ubuf__ int8_t*)get_imm(0x0000);
    __ubuf__ int8_t* ubC = (__ubuf__ int8_t*)get_imm(0x4000);
    __ubuf__ int8_t* ubA = (__ubuf__ int8_t*)get_imm(0x8000);
    __ubuf__ uint32_t* offsets = (__ubuf__ uint32_t*)get_imm(0xc000);
    nddma_desc desc(nddma_desc::loop_desc(4096, 1, 1));
    nddma_out_to_ub(ubB, B, 0, desc, 0, NEAREST_PADDING, 0);
    nddma_out_to_ub(ubC, C, 0, desc, 0, NEAREST_PADDING, 0);
    set_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    wait_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    __VEC_SCOPE__ {
        vector_bool m = pset_b8(PAT_ALL);
        vector_bool out_mask = pset_b8(PAT_ALL);
        #pragma unroll(1)
        for (uint16_t group = 0; group < 2; ++group) {
            vector_s8 a0;
            vector_s8 d0;
            vector_s8 a1;
            vector_s8 d1;
            vector_s8 a2;
            vector_s8 d2;
            vector_s8 a3;
            vector_s8 d3;
            vector_s8 a4;
            vector_s8 d4;
            vector_s8 a5;
            vector_s8 d5;
            vector_s8 a6;
            vector_s8 d6;
            vector_s8 a7;
            vector_s8 d7;
            vlds(a0, ubB, (group * 8 + 0) * 256, NORM);
            vlds(a1, ubB, (group * 8 + 1) * 256, NORM);
            vlds(a2, ubB, (group * 8 + 2) * 256, NORM);
            vlds(a3, ubB, (group * 8 + 3) * 256, NORM);
            vlds(a4, ubB, (group * 8 + 4) * 256, NORM);
            vlds(a5, ubB, (group * 8 + 5) * 256, NORM);
            vlds(a6, ubB, (group * 8 + 6) * 256, NORM);
            vlds(a7, ubB, (group * 8 + 7) * 256, NORM);
            // TARGET 0: VBR (ii)
            vbr(d0, (int8_t)1);
            // TARGET 1: VBR (ii)
            vbr(d1, (int8_t)2);
            // TARGET 2: VBR (ii)
            vbr(d2, (int8_t)3);
            // TARGET 3: VBR (ii)
            vbr(d3, (int8_t)4);
            // TARGET 4: VBR (ii)
            vbr(d4, (int8_t)5);
            // TARGET 5: VBR (ii)
            vbr(d5, (int8_t)6);
            // TARGET 6: VBR (ii)
            vbr(d6, (int8_t)7);
            // TARGET 7: VBR (ii)
            vbr(d7, (int8_t)8);
            vsts(d0, ubA, (group * 8 + 0) * 256, NORM_B8, out_mask);
            vsts(d1, ubA, (group * 8 + 1) * 256, NORM_B8, out_mask);
            vsts(d2, ubA, (group * 8 + 2) * 256, NORM_B8, out_mask);
            vsts(d3, ubA, (group * 8 + 3) * 256, NORM_B8, out_mask);
            vsts(d4, ubA, (group * 8 + 4) * 256, NORM_B8, out_mask);
            vsts(d5, ubA, (group * 8 + 5) * 256, NORM_B8, out_mask);
            vsts(d6, ubA, (group * 8 + 6) * 256, NORM_B8, out_mask);
            vsts(d7, ubA, (group * 8 + 7) * 256, NORM_B8, out_mask);
        }
    }
    offsets[0] = 0;
    pipe_barrier(PIPE_ALL);
    set_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    wait_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    set_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    wait_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    scatter_ubuf_to_gm((__gm__ uint32_t*)A, (__ubuf__ uint32_t*)ubA,
                       0, 0, offsets, 0, 4096, 0, 1);
    set_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
    wait_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
}
