// Generated by benchmarks/a6/generate.py. Edit the generator, then regenerate.
// VSTAS/fp32: No self vector-register RAW path in this form.
// MB_KERNEL bench_vstas_fp32_ii
// MB_TENSOR output A fp32 1024
// MB_TENSOR input B fp32 1024
// MB_TENSOR input C fp32 1024
#ifndef __aicore__
#define __aicore__ [aicore]
#endif
#if __NPU_ARCH__ != 9201
#error "A6 probes require --cce-aicore-arch=dav-920r1-vec"
#endif
extern "C" __global__ __aicore__
void bench_vstas_fp32_ii(__gm__ float* A, __gm__ float* B, __gm__ float* C)
{
    __ubuf__ float* ubB = (__ubuf__ float*)get_imm(0x0000);
    __ubuf__ float* ubC = (__ubuf__ float*)get_imm(0x4000);
    __ubuf__ float* ubA = (__ubuf__ float*)get_imm(0x8000);
    __ubuf__ uint32_t* offsets = (__ubuf__ uint32_t*)get_imm(0xc000);
    nddma_desc desc(nddma_desc::loop_desc(1024, 1, 1));
    nddma_out_to_ub(ubB, B, 0, desc, 0, NEAREST_PADDING, 0);
    nddma_out_to_ub(ubC, C, 0, desc, 0, NEAREST_PADDING, 0);
    set_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    wait_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    __VEC_SCOPE__ {
        vector_bool m = pset_b32(PAT_ALL);
        vector_bool out_mask = pset_b32(PAT_ALL);
        #pragma unroll(1)
        for (uint16_t group = 0; group < 2; ++group) {
            vector_f32 a0;
            vector_f32 d0;
            vector_f32 a1;
            vector_f32 d1;
            vector_f32 a2;
            vector_f32 d2;
            vector_f32 a3;
            vector_f32 d3;
            vector_f32 a4;
            vector_f32 d4;
            vector_f32 a5;
            vector_f32 d5;
            vector_f32 a6;
            vector_f32 d6;
            vector_f32 a7;
            vector_f32 d7;
            vlds(a0, ubB, (group * 8 + 0) * 64, NORM);
            vlds(a1, ubB, (group * 8 + 1) * 64, NORM);
            vlds(a2, ubB, (group * 8 + 2) * 64, NORM);
            vlds(a3, ubB, (group * 8 + 3) * 64, NORM);
            vlds(a4, ubB, (group * 8 + 4) * 64, NORM);
            vlds(a5, ubB, (group * 8 + 5) * 64, NORM);
            vlds(a6, ubB, (group * 8 + 6) * 64, NORM);
            vlds(a7, ubB, (group * 8 + 7) * 64, NORM);
            // TARGET 0: VSTAS (ii)
            vector_align align0 = {};
            __ubuf__ float *ptr0 = ubA + (group * 8 + 0) * 64;
            vstus(align0, 64, a0, ptr0, POST_UPDATE);
            vstas(align0, ptr0, 0);
            // TARGET 1: VSTAS (ii)
            vector_align align1 = {};
            __ubuf__ float *ptr1 = ubA + (group * 8 + 1) * 64;
            vstus(align1, 64, a1, ptr1, POST_UPDATE);
            vstas(align1, ptr1, 0);
            // TARGET 2: VSTAS (ii)
            vector_align align2 = {};
            __ubuf__ float *ptr2 = ubA + (group * 8 + 2) * 64;
            vstus(align2, 64, a2, ptr2, POST_UPDATE);
            vstas(align2, ptr2, 0);
            // TARGET 3: VSTAS (ii)
            vector_align align3 = {};
            __ubuf__ float *ptr3 = ubA + (group * 8 + 3) * 64;
            vstus(align3, 64, a3, ptr3, POST_UPDATE);
            vstas(align3, ptr3, 0);
            // TARGET 4: VSTAS (ii)
            vector_align align4 = {};
            __ubuf__ float *ptr4 = ubA + (group * 8 + 4) * 64;
            vstus(align4, 64, a4, ptr4, POST_UPDATE);
            vstas(align4, ptr4, 0);
            // TARGET 5: VSTAS (ii)
            vector_align align5 = {};
            __ubuf__ float *ptr5 = ubA + (group * 8 + 5) * 64;
            vstus(align5, 64, a5, ptr5, POST_UPDATE);
            vstas(align5, ptr5, 0);
            // TARGET 6: VSTAS (ii)
            vector_align align6 = {};
            __ubuf__ float *ptr6 = ubA + (group * 8 + 6) * 64;
            vstus(align6, 64, a6, ptr6, POST_UPDATE);
            vstas(align6, ptr6, 0);
            // TARGET 7: VSTAS (ii)
            vector_align align7 = {};
            __ubuf__ float *ptr7 = ubA + (group * 8 + 7) * 64;
            vstus(align7, 64, a7, ptr7, POST_UPDATE);
            vstas(align7, ptr7, 0);
        }
    }
    offsets[0] = 0;
    pipe_barrier(PIPE_ALL);
    set_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    wait_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    set_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    wait_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    scatter_ubuf_to_gm((__gm__ uint32_t*)A, (__ubuf__ uint32_t*)ubA,
                       0, 0, offsets, 0, 4096, 0, 1);
    set_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
    wait_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
}
