// Generated by benchmarks/a6/generate.py. Edit the generator, then regenerate.
// VCVT_F32_TO_F16/f32_to_f16: Different input/output types: no type-preserving self RAW probe here; bit-reinterpretation experiments need separate validation.
// MB_KERNEL bench_vcvt_f32_to_f16_f32_to_f16_ii_w8
// MB_TENSOR output A fp16 1024
// MB_TENSOR input B fp32 512
// MB_TENSOR input C fp32 512
#ifndef __aicore__
#define __aicore__ [aicore]
#endif
#if __NPU_ARCH__ != 9201
#error "A6 probes require --cce-aicore-arch=dav-920r1-vec"
#endif
extern "C" __global__ __aicore__
void bench_vcvt_f32_to_f16_f32_to_f16_ii_w8(__gm__ half* A, __gm__ float* B, __gm__ float* C)
{
    __ubuf__ float* ubB = (__ubuf__ float*)get_imm(0x0000);
    __ubuf__ float* ubC = (__ubuf__ float*)get_imm(0x4000);
    __ubuf__ half* ubA = (__ubuf__ half*)get_imm(0x8000);
    __ubuf__ uint32_t* offsets = (__ubuf__ uint32_t*)get_imm(0xc000);
    nddma_desc desc(nddma_desc::loop_desc(512, 1, 1));
    nddma_out_to_ub(ubB, B, 0, desc, 0, NEAREST_PADDING, 0);
    nddma_out_to_ub(ubC, C, 0, desc, 0, NEAREST_PADDING, 0);
    set_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    wait_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    __VEC_SCOPE__ {
        vector_bool m = pset_b32(PAT_ALL);
        vector_bool out_mask = pset_b16(PAT_ALL);
        vector_f32 a0;
        vector_f16 d0;
        vector_f32 a1;
        vector_f16 d1;
        vector_f32 a2;
        vector_f16 d2;
        vector_f32 a3;
        vector_f16 d3;
        vector_f32 a4;
        vector_f16 d4;
        vector_f32 a5;
        vector_f16 d5;
        vector_f32 a6;
        vector_f16 d6;
        vector_f32 a7;
        vector_f16 d7;
        vlds(a0, ubB, 0, NORM);
        vlds(a1, ubB, 64, NORM);
        vlds(a2, ubB, 128, NORM);
        vlds(a3, ubB, 192, NORM);
        vlds(a4, ubB, 256, NORM);
        vlds(a5, ubB, 320, NORM);
        vlds(a6, ubB, 384, NORM);
        vlds(a7, ubB, 448, NORM);
        // TARGET 0: VCVT_F32_TO_F16 (ii)
        vcvt(d0, a0, m, ROUND_R, RS_ENABLE, PART_EVEN);
        // TARGET 1: VCVT_F32_TO_F16 (ii)
        vcvt(d1, a1, m, ROUND_R, RS_ENABLE, PART_EVEN);
        // TARGET 2: VCVT_F32_TO_F16 (ii)
        vcvt(d2, a2, m, ROUND_R, RS_ENABLE, PART_EVEN);
        // TARGET 3: VCVT_F32_TO_F16 (ii)
        vcvt(d3, a3, m, ROUND_R, RS_ENABLE, PART_EVEN);
        // TARGET 4: VCVT_F32_TO_F16 (ii)
        vcvt(d4, a4, m, ROUND_R, RS_ENABLE, PART_EVEN);
        // TARGET 5: VCVT_F32_TO_F16 (ii)
        vcvt(d5, a5, m, ROUND_R, RS_ENABLE, PART_EVEN);
        // TARGET 6: VCVT_F32_TO_F16 (ii)
        vcvt(d6, a6, m, ROUND_R, RS_ENABLE, PART_EVEN);
        // TARGET 7: VCVT_F32_TO_F16 (ii)
        vcvt(d7, a7, m, ROUND_R, RS_ENABLE, PART_EVEN);
        vsts(d0, ubA, 0, NORM_B16, out_mask);
        vsts(d1, ubA, 128, NORM_B16, out_mask);
        vsts(d2, ubA, 256, NORM_B16, out_mask);
        vsts(d3, ubA, 384, NORM_B16, out_mask);
        vsts(d4, ubA, 512, NORM_B16, out_mask);
        vsts(d5, ubA, 640, NORM_B16, out_mask);
        vsts(d6, ubA, 768, NORM_B16, out_mask);
        vsts(d7, ubA, 896, NORM_B16, out_mask);
    }
    offsets[0] = 0;
    pipe_barrier(PIPE_ALL);
    set_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    wait_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    set_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    wait_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    scatter_ubuf_to_gm((__gm__ uint32_t*)A, (__ubuf__ uint32_t*)ubA,
                       0, 0, offsets, 0, 2048, 0, 1);
    set_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
    wait_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
}
