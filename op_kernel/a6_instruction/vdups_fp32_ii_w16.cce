// Generated by benchmarks/a6/generate.py. Edit the generator, then regenerate.
// VDUPS/fp32: No self vector-register RAW path in this form.
// MB_KERNEL bench_vdups_fp32_ii_w16
// MB_TENSOR output A fp32 1024
// MB_TENSOR input B fp32 1024
// MB_TENSOR input C fp32 1024
#ifndef __aicore__
#define __aicore__ [aicore]
#endif
#if __NPU_ARCH__ != 9201
#error "A6 probes require --cce-aicore-arch=dav-920r1-vec"
#endif
extern "C" __global__ __aicore__
void bench_vdups_fp32_ii_w16(__gm__ float* A, __gm__ float* B, __gm__ float* C)
{
    __ubuf__ float* ubB = (__ubuf__ float*)get_imm(0x0000);
    __ubuf__ float* ubC = (__ubuf__ float*)get_imm(0x4000);
    __ubuf__ float* ubA = (__ubuf__ float*)get_imm(0x8000);
    __ubuf__ uint32_t* offsets = (__ubuf__ uint32_t*)get_imm(0xc000);
    nddma_desc desc(nddma_desc::loop_desc(1024, 1, 1));
    nddma_out_to_ub(ubB, B, 0, desc, 0, NEAREST_PADDING, 0);
    nddma_out_to_ub(ubC, C, 0, desc, 0, NEAREST_PADDING, 0);
    set_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    wait_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    __VEC_SCOPE__ {
        vector_bool m = pset_b32(PAT_ALL);
        vector_bool out_mask = pset_b32(PAT_ALL);
        vector_f32 a0;
        vector_f32 d0;
        vector_f32 a1;
        vector_f32 d1;
        vector_f32 a2;
        vector_f32 d2;
        vector_f32 a3;
        vector_f32 d3;
        vector_f32 a4;
        vector_f32 d4;
        vector_f32 a5;
        vector_f32 d5;
        vector_f32 a6;
        vector_f32 d6;
        vector_f32 a7;
        vector_f32 d7;
        vector_f32 a8;
        vector_f32 d8;
        vector_f32 a9;
        vector_f32 d9;
        vector_f32 a10;
        vector_f32 d10;
        vector_f32 a11;
        vector_f32 d11;
        vector_f32 a12;
        vector_f32 d12;
        vector_f32 a13;
        vector_f32 d13;
        vector_f32 a14;
        vector_f32 d14;
        vector_f32 a15;
        vector_f32 d15;
        vlds(a0, ubB, 0, NORM);
        vlds(a1, ubB, 64, NORM);
        vlds(a2, ubB, 128, NORM);
        vlds(a3, ubB, 192, NORM);
        vlds(a4, ubB, 256, NORM);
        vlds(a5, ubB, 320, NORM);
        vlds(a6, ubB, 384, NORM);
        vlds(a7, ubB, 448, NORM);
        vlds(a8, ubB, 512, NORM);
        vlds(a9, ubB, 576, NORM);
        vlds(a10, ubB, 640, NORM);
        vlds(a11, ubB, 704, NORM);
        vlds(a12, ubB, 768, NORM);
        vlds(a13, ubB, 832, NORM);
        vlds(a14, ubB, 896, NORM);
        vlds(a15, ubB, 960, NORM);
        // TARGET 0: VDUPS (ii)
        vdup(d0, (float)1, m, MODE_ZEROING);
        // TARGET 1: VDUPS (ii)
        vdup(d1, (float)2, m, MODE_ZEROING);
        // TARGET 2: VDUPS (ii)
        vdup(d2, (float)3, m, MODE_ZEROING);
        // TARGET 3: VDUPS (ii)
        vdup(d3, (float)4, m, MODE_ZEROING);
        // TARGET 4: VDUPS (ii)
        vdup(d4, (float)5, m, MODE_ZEROING);
        // TARGET 5: VDUPS (ii)
        vdup(d5, (float)6, m, MODE_ZEROING);
        // TARGET 6: VDUPS (ii)
        vdup(d6, (float)7, m, MODE_ZEROING);
        // TARGET 7: VDUPS (ii)
        vdup(d7, (float)8, m, MODE_ZEROING);
        // TARGET 8: VDUPS (ii)
        vdup(d8, (float)9, m, MODE_ZEROING);
        // TARGET 9: VDUPS (ii)
        vdup(d9, (float)10, m, MODE_ZEROING);
        // TARGET 10: VDUPS (ii)
        vdup(d10, (float)11, m, MODE_ZEROING);
        // TARGET 11: VDUPS (ii)
        vdup(d11, (float)12, m, MODE_ZEROING);
        // TARGET 12: VDUPS (ii)
        vdup(d12, (float)13, m, MODE_ZEROING);
        // TARGET 13: VDUPS (ii)
        vdup(d13, (float)14, m, MODE_ZEROING);
        // TARGET 14: VDUPS (ii)
        vdup(d14, (float)15, m, MODE_ZEROING);
        // TARGET 15: VDUPS (ii)
        vdup(d15, (float)16, m, MODE_ZEROING);
        vsts(d0, ubA, 0, NORM_B32, out_mask);
        vsts(d1, ubA, 64, NORM_B32, out_mask);
        vsts(d2, ubA, 128, NORM_B32, out_mask);
        vsts(d3, ubA, 192, NORM_B32, out_mask);
        vsts(d4, ubA, 256, NORM_B32, out_mask);
        vsts(d5, ubA, 320, NORM_B32, out_mask);
        vsts(d6, ubA, 384, NORM_B32, out_mask);
        vsts(d7, ubA, 448, NORM_B32, out_mask);
        vsts(d8, ubA, 512, NORM_B32, out_mask);
        vsts(d9, ubA, 576, NORM_B32, out_mask);
        vsts(d10, ubA, 640, NORM_B32, out_mask);
        vsts(d11, ubA, 704, NORM_B32, out_mask);
        vsts(d12, ubA, 768, NORM_B32, out_mask);
        vsts(d13, ubA, 832, NORM_B32, out_mask);
        vsts(d14, ubA, 896, NORM_B32, out_mask);
        vsts(d15, ubA, 960, NORM_B32, out_mask);
    }
    offsets[0] = 0;
    pipe_barrier(PIPE_ALL);
    set_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    wait_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    set_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    wait_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    scatter_ubuf_to_gm((__gm__ uint32_t*)A, (__ubuf__ uint32_t*)ubA,
                       0, 0, offsets, 0, 4096, 0, 1);
    set_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
    wait_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
}
