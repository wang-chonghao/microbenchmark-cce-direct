// Generated by benchmarks/a6/generate.py. Edit the generator, then regenerate.
// VLDS/fp16: No self vector-register RAW path in this form.
// MB_KERNEL bench_vlds_fp16_ii
// MB_TENSOR output A fp16 2048
// MB_TENSOR input B fp16 2048
// MB_TENSOR input C fp16 2048
#ifndef __aicore__
#define __aicore__ [aicore]
#endif
#if __NPU_ARCH__ != 9201
#error "A6 probes require --cce-aicore-arch=dav-920r1-vec"
#endif
extern "C" __global__ __aicore__
void bench_vlds_fp16_ii(__gm__ half* A, __gm__ half* B, __gm__ half* C)
{
    __ubuf__ half* ubB = (__ubuf__ half*)get_imm(0x0000);
    __ubuf__ half* ubC = (__ubuf__ half*)get_imm(0x4000);
    __ubuf__ half* ubA = (__ubuf__ half*)get_imm(0x8000);
    __ubuf__ uint32_t* offsets = (__ubuf__ uint32_t*)get_imm(0xc000);
    nddma_desc desc(nddma_desc::loop_desc(2048, 1, 1));
    nddma_out_to_ub(ubB, B, 0, desc, 0, NEAREST_PADDING, 0);
    nddma_out_to_ub(ubC, C, 0, desc, 0, NEAREST_PADDING, 0);
    set_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    wait_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    __VEC_SCOPE__ {
        vector_bool m = pset_b16(PAT_ALL);
        vector_bool out_mask = pset_b16(PAT_ALL);
        #pragma unroll(1)
        for (uint16_t group = 0; group < 2; ++group) {
            vector_f16 a0;
            vector_f16 d0;
            vector_f16 a1;
            vector_f16 d1;
            vector_f16 a2;
            vector_f16 d2;
            vector_f16 a3;
            vector_f16 d3;
            vector_f16 a4;
            vector_f16 d4;
            vector_f16 a5;
            vector_f16 d5;
            vector_f16 a6;
            vector_f16 d6;
            vector_f16 a7;
            vector_f16 d7;
            // TARGET 0: VLDS (ii)
            vlds(d0, ubB, (group * 8 + 0) * 128, NORM);
            // TARGET 1: VLDS (ii)
            vlds(d1, ubB, (group * 8 + 1) * 128, NORM);
            // TARGET 2: VLDS (ii)
            vlds(d2, ubB, (group * 8 + 2) * 128, NORM);
            // TARGET 3: VLDS (ii)
            vlds(d3, ubB, (group * 8 + 3) * 128, NORM);
            // TARGET 4: VLDS (ii)
            vlds(d4, ubB, (group * 8 + 4) * 128, NORM);
            // TARGET 5: VLDS (ii)
            vlds(d5, ubB, (group * 8 + 5) * 128, NORM);
            // TARGET 6: VLDS (ii)
            vlds(d6, ubB, (group * 8 + 6) * 128, NORM);
            // TARGET 7: VLDS (ii)
            vlds(d7, ubB, (group * 8 + 7) * 128, NORM);
            vsts(d0, ubA, (group * 8 + 0) * 128, NORM_B16, out_mask);
            vsts(d1, ubA, (group * 8 + 1) * 128, NORM_B16, out_mask);
            vsts(d2, ubA, (group * 8 + 2) * 128, NORM_B16, out_mask);
            vsts(d3, ubA, (group * 8 + 3) * 128, NORM_B16, out_mask);
            vsts(d4, ubA, (group * 8 + 4) * 128, NORM_B16, out_mask);
            vsts(d5, ubA, (group * 8 + 5) * 128, NORM_B16, out_mask);
            vsts(d6, ubA, (group * 8 + 6) * 128, NORM_B16, out_mask);
            vsts(d7, ubA, (group * 8 + 7) * 128, NORM_B16, out_mask);
        }
    }
    offsets[0] = 0;
    pipe_barrier(PIPE_ALL);
    set_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    wait_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    set_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    wait_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    scatter_ubuf_to_gm((__gm__ uint32_t*)A, (__ubuf__ uint32_t*)ubA,
                       0, 0, offsets, 0, 4096, 0, 1);
    set_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
    wait_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
}
