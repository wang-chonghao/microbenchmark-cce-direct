// Generated by benchmarks/a6/generate.py. Edit the generator, then regenerate.
// VCVT_F16_TO_F32/f16_to_f32: Different input/output types: no type-preserving self RAW probe here; bit-reinterpretation experiments need separate validation.
// MB_KERNEL bench_vcvt_f16_to_f32_f16_to_f32_ii
// MB_TENSOR output A fp32 1024
// MB_TENSOR input B fp16 2048
// MB_TENSOR input C fp16 2048
#ifndef __aicore__
#define __aicore__ [aicore]
#endif
#if __NPU_ARCH__ != 9201
#error "A6 probes require --cce-aicore-arch=dav-920r1-vec"
#endif
extern "C" __global__ __aicore__
void bench_vcvt_f16_to_f32_f16_to_f32_ii(__gm__ float* A, __gm__ half* B, __gm__ half* C)
{
    __ubuf__ half* ubB = (__ubuf__ half*)get_imm(0x0000);
    __ubuf__ half* ubC = (__ubuf__ half*)get_imm(0x4000);
    __ubuf__ float* ubA = (__ubuf__ float*)get_imm(0x8000);
    __ubuf__ uint32_t* offsets = (__ubuf__ uint32_t*)get_imm(0xc000);
    nddma_desc desc(nddma_desc::loop_desc(2048, 1, 1));
    nddma_out_to_ub(ubB, B, 0, desc, 0, NEAREST_PADDING, 0);
    nddma_out_to_ub(ubC, C, 0, desc, 0, NEAREST_PADDING, 0);
    set_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    wait_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    __VEC_SCOPE__ {
        vector_bool m = pset_b16(PAT_ALL);
        vector_bool out_mask = pset_b32(PAT_ALL);
        #pragma unroll(1)
        for (uint16_t group = 0; group < 2; ++group) {
            vector_f16 a0;
            vector_f32 d0;
            vector_f16 a1;
            vector_f32 d1;
            vector_f16 a2;
            vector_f32 d2;
            vector_f16 a3;
            vector_f32 d3;
            vector_f16 a4;
            vector_f32 d4;
            vector_f16 a5;
            vector_f32 d5;
            vector_f16 a6;
            vector_f32 d6;
            vector_f16 a7;
            vector_f32 d7;
            vlds(a0, ubB, (group * 8 + 0) * 128, NORM);
            vlds(a1, ubB, (group * 8 + 1) * 128, NORM);
            vlds(a2, ubB, (group * 8 + 2) * 128, NORM);
            vlds(a3, ubB, (group * 8 + 3) * 128, NORM);
            vlds(a4, ubB, (group * 8 + 4) * 128, NORM);
            vlds(a5, ubB, (group * 8 + 5) * 128, NORM);
            vlds(a6, ubB, (group * 8 + 6) * 128, NORM);
            vlds(a7, ubB, (group * 8 + 7) * 128, NORM);
            // TARGET 0: VCVT_F16_TO_F32 (ii)
            vcvt(d0, a0, m, PART_EVEN, MODE_ZEROING);
            // TARGET 1: VCVT_F16_TO_F32 (ii)
            vcvt(d1, a1, m, PART_EVEN, MODE_ZEROING);
            // TARGET 2: VCVT_F16_TO_F32 (ii)
            vcvt(d2, a2, m, PART_EVEN, MODE_ZEROING);
            // TARGET 3: VCVT_F16_TO_F32 (ii)
            vcvt(d3, a3, m, PART_EVEN, MODE_ZEROING);
            // TARGET 4: VCVT_F16_TO_F32 (ii)
            vcvt(d4, a4, m, PART_EVEN, MODE_ZEROING);
            // TARGET 5: VCVT_F16_TO_F32 (ii)
            vcvt(d5, a5, m, PART_EVEN, MODE_ZEROING);
            // TARGET 6: VCVT_F16_TO_F32 (ii)
            vcvt(d6, a6, m, PART_EVEN, MODE_ZEROING);
            // TARGET 7: VCVT_F16_TO_F32 (ii)
            vcvt(d7, a7, m, PART_EVEN, MODE_ZEROING);
            vsts(d0, ubA, (group * 8 + 0) * 64, NORM_B32, out_mask);
            vsts(d1, ubA, (group * 8 + 1) * 64, NORM_B32, out_mask);
            vsts(d2, ubA, (group * 8 + 2) * 64, NORM_B32, out_mask);
            vsts(d3, ubA, (group * 8 + 3) * 64, NORM_B32, out_mask);
            vsts(d4, ubA, (group * 8 + 4) * 64, NORM_B32, out_mask);
            vsts(d5, ubA, (group * 8 + 5) * 64, NORM_B32, out_mask);
            vsts(d6, ubA, (group * 8 + 6) * 64, NORM_B32, out_mask);
            vsts(d7, ubA, (group * 8 + 7) * 64, NORM_B32, out_mask);
        }
    }
    offsets[0] = 0;
    pipe_barrier(PIPE_ALL);
    set_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    wait_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    set_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    wait_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    scatter_ubuf_to_gm((__gm__ uint32_t*)A, (__ubuf__ uint32_t*)ubA,
                       0, 0, offsets, 0, 4096, 0, 1);
    set_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
    wait_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
}
