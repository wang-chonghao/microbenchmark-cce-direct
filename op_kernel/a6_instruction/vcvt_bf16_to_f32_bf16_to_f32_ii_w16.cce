// Generated by benchmarks/a6/generate.py. Edit the generator, then regenerate.
// VCVT_BF16_TO_F32/bf16_to_f32: Different input/output types: no type-preserving self RAW probe here; bit-reinterpretation experiments need separate validation.
// MB_KERNEL bench_vcvt_bf16_to_f32_bf16_to_f32_ii_w16
// MB_TENSOR output A fp32 1024
// MB_TENSOR input B bf16 2048
// MB_TENSOR input C bf16 2048
#ifndef __aicore__
#define __aicore__ [aicore]
#endif
#if __NPU_ARCH__ != 9201
#error "A6 probes require --cce-aicore-arch=dav-920r1-vec"
#endif
extern "C" __global__ __aicore__
void bench_vcvt_bf16_to_f32_bf16_to_f32_ii_w16(__gm__ float* A, __gm__ bfloat16_t* B, __gm__ bfloat16_t* C)
{
    __ubuf__ bfloat16_t* ubB = (__ubuf__ bfloat16_t*)get_imm(0x0000);
    __ubuf__ bfloat16_t* ubC = (__ubuf__ bfloat16_t*)get_imm(0x4000);
    __ubuf__ float* ubA = (__ubuf__ float*)get_imm(0x8000);
    __ubuf__ uint32_t* offsets = (__ubuf__ uint32_t*)get_imm(0xc000);
    nddma_desc desc(nddma_desc::loop_desc(2048, 1, 1));
    nddma_out_to_ub(ubB, B, 0, desc, 0, NEAREST_PADDING, 0);
    nddma_out_to_ub(ubC, C, 0, desc, 0, NEAREST_PADDING, 0);
    set_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    wait_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    __VEC_SCOPE__ {
        vector_bool m = pset_b16(PAT_ALL);
        vector_bool out_mask = pset_b32(PAT_ALL);
        vector_bf16 a0;
        vector_f32 d0;
        vector_bf16 a1;
        vector_f32 d1;
        vector_bf16 a2;
        vector_f32 d2;
        vector_bf16 a3;
        vector_f32 d3;
        vector_bf16 a4;
        vector_f32 d4;
        vector_bf16 a5;
        vector_f32 d5;
        vector_bf16 a6;
        vector_f32 d6;
        vector_bf16 a7;
        vector_f32 d7;
        vector_bf16 a8;
        vector_f32 d8;
        vector_bf16 a9;
        vector_f32 d9;
        vector_bf16 a10;
        vector_f32 d10;
        vector_bf16 a11;
        vector_f32 d11;
        vector_bf16 a12;
        vector_f32 d12;
        vector_bf16 a13;
        vector_f32 d13;
        vector_bf16 a14;
        vector_f32 d14;
        vector_bf16 a15;
        vector_f32 d15;
        vlds(a0, ubB, 0, NORM);
        vlds(a1, ubB, 128, NORM);
        vlds(a2, ubB, 256, NORM);
        vlds(a3, ubB, 384, NORM);
        vlds(a4, ubB, 512, NORM);
        vlds(a5, ubB, 640, NORM);
        vlds(a6, ubB, 768, NORM);
        vlds(a7, ubB, 896, NORM);
        vlds(a8, ubB, 1024, NORM);
        vlds(a9, ubB, 1152, NORM);
        vlds(a10, ubB, 1280, NORM);
        vlds(a11, ubB, 1408, NORM);
        vlds(a12, ubB, 1536, NORM);
        vlds(a13, ubB, 1664, NORM);
        vlds(a14, ubB, 1792, NORM);
        vlds(a15, ubB, 1920, NORM);
        // TARGET 0: VCVT_BF16_TO_F32 (ii)
        vcvt(d0, a0, m, PART_EVEN, MODE_ZEROING);
        // TARGET 1: VCVT_BF16_TO_F32 (ii)
        vcvt(d1, a1, m, PART_EVEN, MODE_ZEROING);
        // TARGET 2: VCVT_BF16_TO_F32 (ii)
        vcvt(d2, a2, m, PART_EVEN, MODE_ZEROING);
        // TARGET 3: VCVT_BF16_TO_F32 (ii)
        vcvt(d3, a3, m, PART_EVEN, MODE_ZEROING);
        // TARGET 4: VCVT_BF16_TO_F32 (ii)
        vcvt(d4, a4, m, PART_EVEN, MODE_ZEROING);
        // TARGET 5: VCVT_BF16_TO_F32 (ii)
        vcvt(d5, a5, m, PART_EVEN, MODE_ZEROING);
        // TARGET 6: VCVT_BF16_TO_F32 (ii)
        vcvt(d6, a6, m, PART_EVEN, MODE_ZEROING);
        // TARGET 7: VCVT_BF16_TO_F32 (ii)
        vcvt(d7, a7, m, PART_EVEN, MODE_ZEROING);
        // TARGET 8: VCVT_BF16_TO_F32 (ii)
        vcvt(d8, a8, m, PART_EVEN, MODE_ZEROING);
        // TARGET 9: VCVT_BF16_TO_F32 (ii)
        vcvt(d9, a9, m, PART_EVEN, MODE_ZEROING);
        // TARGET 10: VCVT_BF16_TO_F32 (ii)
        vcvt(d10, a10, m, PART_EVEN, MODE_ZEROING);
        // TARGET 11: VCVT_BF16_TO_F32 (ii)
        vcvt(d11, a11, m, PART_EVEN, MODE_ZEROING);
        // TARGET 12: VCVT_BF16_TO_F32 (ii)
        vcvt(d12, a12, m, PART_EVEN, MODE_ZEROING);
        // TARGET 13: VCVT_BF16_TO_F32 (ii)
        vcvt(d13, a13, m, PART_EVEN, MODE_ZEROING);
        // TARGET 14: VCVT_BF16_TO_F32 (ii)
        vcvt(d14, a14, m, PART_EVEN, MODE_ZEROING);
        // TARGET 15: VCVT_BF16_TO_F32 (ii)
        vcvt(d15, a15, m, PART_EVEN, MODE_ZEROING);
        vsts(d0, ubA, 0, NORM_B32, out_mask);
        vsts(d1, ubA, 64, NORM_B32, out_mask);
        vsts(d2, ubA, 128, NORM_B32, out_mask);
        vsts(d3, ubA, 192, NORM_B32, out_mask);
        vsts(d4, ubA, 256, NORM_B32, out_mask);
        vsts(d5, ubA, 320, NORM_B32, out_mask);
        vsts(d6, ubA, 384, NORM_B32, out_mask);
        vsts(d7, ubA, 448, NORM_B32, out_mask);
        vsts(d8, ubA, 512, NORM_B32, out_mask);
        vsts(d9, ubA, 576, NORM_B32, out_mask);
        vsts(d10, ubA, 640, NORM_B32, out_mask);
        vsts(d11, ubA, 704, NORM_B32, out_mask);
        vsts(d12, ubA, 768, NORM_B32, out_mask);
        vsts(d13, ubA, 832, NORM_B32, out_mask);
        vsts(d14, ubA, 896, NORM_B32, out_mask);
        vsts(d15, ubA, 960, NORM_B32, out_mask);
    }
    offsets[0] = 0;
    pipe_barrier(PIPE_ALL);
    set_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    wait_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    set_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    wait_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    scatter_ubuf_to_gm((__gm__ uint32_t*)A, (__ubuf__ uint32_t*)ubA,
                       0, 0, offsets, 0, 4096, 0, 1);
    set_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
    wait_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
}
