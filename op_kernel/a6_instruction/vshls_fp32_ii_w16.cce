// Generated by benchmarks/a6/generate.py. Edit the generator, then regenerate.
// VSHLS/fp32: ISA fp label denotes bit width here: unsigned integer shift, not floating arithmetic.
// MB_KERNEL bench_vshls_fp32_ii_w16
// MB_TENSOR output A uint32 1024
// MB_TENSOR input B uint32 1024
// MB_TENSOR input C uint32 1024
#ifndef __aicore__
#define __aicore__ [aicore]
#endif
#if __NPU_ARCH__ != 9201
#error "A6 probes require --cce-aicore-arch=dav-920r1-vec"
#endif
extern "C" __global__ __aicore__
void bench_vshls_fp32_ii_w16(__gm__ uint32_t* A, __gm__ uint32_t* B, __gm__ uint32_t* C)
{
    __ubuf__ uint32_t* ubB = (__ubuf__ uint32_t*)get_imm(0x0000);
    __ubuf__ uint32_t* ubC = (__ubuf__ uint32_t*)get_imm(0x4000);
    __ubuf__ uint32_t* ubA = (__ubuf__ uint32_t*)get_imm(0x8000);
    __ubuf__ uint32_t* offsets = (__ubuf__ uint32_t*)get_imm(0xc000);
    nddma_desc desc(nddma_desc::loop_desc(1024, 1, 1));
    nddma_out_to_ub(ubB, B, 0, desc, 0, NEAREST_PADDING, 0);
    nddma_out_to_ub(ubC, C, 0, desc, 0, NEAREST_PADDING, 0);
    set_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    wait_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    __VEC_SCOPE__ {
        vector_bool m = pset_b32(PAT_ALL);
        vector_bool out_mask = pset_b32(PAT_ALL);
        vector_u32 a0;
        vector_u32 d0;
        vector_u32 a1;
        vector_u32 d1;
        vector_u32 a2;
        vector_u32 d2;
        vector_u32 a3;
        vector_u32 d3;
        vector_u32 a4;
        vector_u32 d4;
        vector_u32 a5;
        vector_u32 d5;
        vector_u32 a6;
        vector_u32 d6;
        vector_u32 a7;
        vector_u32 d7;
        vector_u32 a8;
        vector_u32 d8;
        vector_u32 a9;
        vector_u32 d9;
        vector_u32 a10;
        vector_u32 d10;
        vector_u32 a11;
        vector_u32 d11;
        vector_u32 a12;
        vector_u32 d12;
        vector_u32 a13;
        vector_u32 d13;
        vector_u32 a14;
        vector_u32 d14;
        vector_u32 a15;
        vector_u32 d15;
        vlds(a0, ubB, 0, NORM);
        vlds(a1, ubB, 64, NORM);
        vlds(a2, ubB, 128, NORM);
        vlds(a3, ubB, 192, NORM);
        vlds(a4, ubB, 256, NORM);
        vlds(a5, ubB, 320, NORM);
        vlds(a6, ubB, 384, NORM);
        vlds(a7, ubB, 448, NORM);
        vlds(a8, ubB, 512, NORM);
        vlds(a9, ubB, 576, NORM);
        vlds(a10, ubB, 640, NORM);
        vlds(a11, ubB, 704, NORM);
        vlds(a12, ubB, 768, NORM);
        vlds(a13, ubB, 832, NORM);
        vlds(a14, ubB, 896, NORM);
        vlds(a15, ubB, 960, NORM);
        // TARGET 0: VSHLS (ii)
        vshls(d0, a0, (uint32_t)1, m);
        // TARGET 1: VSHLS (ii)
        vshls(d1, a1, (uint32_t)1, m);
        // TARGET 2: VSHLS (ii)
        vshls(d2, a2, (uint32_t)1, m);
        // TARGET 3: VSHLS (ii)
        vshls(d3, a3, (uint32_t)1, m);
        // TARGET 4: VSHLS (ii)
        vshls(d4, a4, (uint32_t)1, m);
        // TARGET 5: VSHLS (ii)
        vshls(d5, a5, (uint32_t)1, m);
        // TARGET 6: VSHLS (ii)
        vshls(d6, a6, (uint32_t)1, m);
        // TARGET 7: VSHLS (ii)
        vshls(d7, a7, (uint32_t)1, m);
        // TARGET 8: VSHLS (ii)
        vshls(d8, a8, (uint32_t)1, m);
        // TARGET 9: VSHLS (ii)
        vshls(d9, a9, (uint32_t)1, m);
        // TARGET 10: VSHLS (ii)
        vshls(d10, a10, (uint32_t)1, m);
        // TARGET 11: VSHLS (ii)
        vshls(d11, a11, (uint32_t)1, m);
        // TARGET 12: VSHLS (ii)
        vshls(d12, a12, (uint32_t)1, m);
        // TARGET 13: VSHLS (ii)
        vshls(d13, a13, (uint32_t)1, m);
        // TARGET 14: VSHLS (ii)
        vshls(d14, a14, (uint32_t)1, m);
        // TARGET 15: VSHLS (ii)
        vshls(d15, a15, (uint32_t)1, m);
        vsts(d0, ubA, 0, NORM_B32, out_mask);
        vsts(d1, ubA, 64, NORM_B32, out_mask);
        vsts(d2, ubA, 128, NORM_B32, out_mask);
        vsts(d3, ubA, 192, NORM_B32, out_mask);
        vsts(d4, ubA, 256, NORM_B32, out_mask);
        vsts(d5, ubA, 320, NORM_B32, out_mask);
        vsts(d6, ubA, 384, NORM_B32, out_mask);
        vsts(d7, ubA, 448, NORM_B32, out_mask);
        vsts(d8, ubA, 512, NORM_B32, out_mask);
        vsts(d9, ubA, 576, NORM_B32, out_mask);
        vsts(d10, ubA, 640, NORM_B32, out_mask);
        vsts(d11, ubA, 704, NORM_B32, out_mask);
        vsts(d12, ubA, 768, NORM_B32, out_mask);
        vsts(d13, ubA, 832, NORM_B32, out_mask);
        vsts(d14, ubA, 896, NORM_B32, out_mask);
        vsts(d15, ubA, 960, NORM_B32, out_mask);
    }
    offsets[0] = 0;
    pipe_barrier(PIPE_ALL);
    set_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    wait_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    set_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    wait_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    scatter_ubuf_to_gm((__gm__ uint32_t*)A, (__ubuf__ uint32_t*)ubA,
                       0, 0, offsets, 0, 4096, 0, 1);
    set_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
    wait_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
}
