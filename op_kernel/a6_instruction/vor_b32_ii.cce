// Generated by benchmarks/a6/generate.py. Edit the generator, then regenerate.
// VOR/b32:
// MB_KERNEL bench_vor_b32_ii
// MB_TENSOR output A uint32 1024
// MB_TENSOR input B uint32 1024
// MB_TENSOR input C uint32 1024
#ifndef __aicore__
#define __aicore__ [aicore]
#endif
#if __NPU_ARCH__ != 9201
#error "A6 probes require --cce-aicore-arch=dav-920r1-vec"
#endif
extern "C" __global__ __aicore__
void bench_vor_b32_ii(__gm__ uint32_t* A, __gm__ uint32_t* B, __gm__ uint32_t* C)
{
    __ubuf__ uint32_t* ubB = (__ubuf__ uint32_t*)get_imm(0x0000);
    __ubuf__ uint32_t* ubC = (__ubuf__ uint32_t*)get_imm(0x4000);
    __ubuf__ uint32_t* ubA = (__ubuf__ uint32_t*)get_imm(0x8000);
    __ubuf__ uint32_t* offsets = (__ubuf__ uint32_t*)get_imm(0xc000);
    nddma_desc desc(nddma_desc::loop_desc(1024, 1, 1));
    nddma_out_to_ub(ubB, B, 0, desc, 0, NEAREST_PADDING, 0);
    nddma_out_to_ub(ubC, C, 0, desc, 0, NEAREST_PADDING, 0);
    set_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    wait_flag(PIPE_MTE2, PIPE_V, EVENT_ID0);
    __VEC_SCOPE__ {
        vector_bool m = pset_b32(PAT_ALL);
        vector_bool out_mask = pset_b32(PAT_ALL);
        #pragma unroll(1)
        for (uint16_t group = 0; group < 2; ++group) {
            vector_u32 a0;
            vector_u32 d0;
            vector_u32 b0;
            vector_u32 a1;
            vector_u32 d1;
            vector_u32 b1;
            vector_u32 a2;
            vector_u32 d2;
            vector_u32 b2;
            vector_u32 a3;
            vector_u32 d3;
            vector_u32 b3;
            vector_u32 a4;
            vector_u32 d4;
            vector_u32 b4;
            vector_u32 a5;
            vector_u32 d5;
            vector_u32 b5;
            vector_u32 a6;
            vector_u32 d6;
            vector_u32 b6;
            vector_u32 a7;
            vector_u32 d7;
            vector_u32 b7;
            vlds(a0, ubB, (group * 8 + 0) * 64, NORM);
            vlds(b0, ubC, (group * 8 + 0) * 64, NORM);
            vlds(a1, ubB, (group * 8 + 1) * 64, NORM);
            vlds(b1, ubC, (group * 8 + 1) * 64, NORM);
            vlds(a2, ubB, (group * 8 + 2) * 64, NORM);
            vlds(b2, ubC, (group * 8 + 2) * 64, NORM);
            vlds(a3, ubB, (group * 8 + 3) * 64, NORM);
            vlds(b3, ubC, (group * 8 + 3) * 64, NORM);
            vlds(a4, ubB, (group * 8 + 4) * 64, NORM);
            vlds(b4, ubC, (group * 8 + 4) * 64, NORM);
            vlds(a5, ubB, (group * 8 + 5) * 64, NORM);
            vlds(b5, ubC, (group * 8 + 5) * 64, NORM);
            vlds(a6, ubB, (group * 8 + 6) * 64, NORM);
            vlds(b6, ubC, (group * 8 + 6) * 64, NORM);
            vlds(a7, ubB, (group * 8 + 7) * 64, NORM);
            vlds(b7, ubC, (group * 8 + 7) * 64, NORM);
            // TARGET 0: VOR (ii)
            vor(d0, a0, b0, m);
            // TARGET 1: VOR (ii)
            vor(d1, a1, b1, m);
            // TARGET 2: VOR (ii)
            vor(d2, a2, b2, m);
            // TARGET 3: VOR (ii)
            vor(d3, a3, b3, m);
            // TARGET 4: VOR (ii)
            vor(d4, a4, b4, m);
            // TARGET 5: VOR (ii)
            vor(d5, a5, b5, m);
            // TARGET 6: VOR (ii)
            vor(d6, a6, b6, m);
            // TARGET 7: VOR (ii)
            vor(d7, a7, b7, m);
            vsts(d0, ubA, (group * 8 + 0) * 64, NORM_B32, out_mask);
            vsts(d1, ubA, (group * 8 + 1) * 64, NORM_B32, out_mask);
            vsts(d2, ubA, (group * 8 + 2) * 64, NORM_B32, out_mask);
            vsts(d3, ubA, (group * 8 + 3) * 64, NORM_B32, out_mask);
            vsts(d4, ubA, (group * 8 + 4) * 64, NORM_B32, out_mask);
            vsts(d5, ubA, (group * 8 + 5) * 64, NORM_B32, out_mask);
            vsts(d6, ubA, (group * 8 + 6) * 64, NORM_B32, out_mask);
            vsts(d7, ubA, (group * 8 + 7) * 64, NORM_B32, out_mask);
        }
    }
    offsets[0] = 0;
    pipe_barrier(PIPE_ALL);
    set_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    wait_flag(PIPE_V, PIPE_MTE3, EVENT_ID0);
    set_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    wait_flag(PIPE_S, PIPE_MTE3, EVENT_ID1);
    scatter_ubuf_to_gm((__gm__ uint32_t*)A, (__ubuf__ uint32_t*)ubA,
                       0, 0, offsets, 0, 4096, 0, 1);
    set_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
    wait_flag(PIPE_MTE3, PIPE_S, EVENT_ID0);
}
